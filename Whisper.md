# Whisper

Setup and configuration for a local whisper.cpp speech-to-text server with CUDA acceleration.

**Repo:** `~/Git/Whisper`

Documentation-only repo containing the server setup guide for whisper.cpp. Provides an HTTP API for audio transcription on the local GPU. Used by other services in the corpus that need speech-to-text.
