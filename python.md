# Python

Projects in this corpus written in Python — mostly protocol fuzzers (via boofuzz), document-processing services, and small automation scripts where an existing Python library pays for itself.
