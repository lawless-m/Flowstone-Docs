# DuckDB

Projects using DuckDB for analytics, vector similarity search, Parquet ingestion, or other columnar workloads. An in-process SQL engine that sits in the same niche as SQLite but aimed at analytical rather than transactional queries.
