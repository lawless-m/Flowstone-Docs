# wasm

Projects in this corpus that ship a WebAssembly target so that the code can run inside a browser tab.

## Members

- [[cozordb]] — `cozo-lib-wasm` exposes a `CozoDb` JS class over the in-memory backend, including in-RAM full-text search
- [[Ztil]] — Research Machines 380Z emulator with a WASM build that runs the machine in a browser

## Related themes

- [[rust]]
- [[full-text-search]]
